CreateThread(function()
    if Inventory == 'esx_inv' then
        -- Since there are multiple versions of esx_inventory, you'll need to add the stash exports and events from your specific version.
    end
end)